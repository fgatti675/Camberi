Drop `francesco`, `mattia` or `marian` here as `.jpg`, `.jpeg`, `.png` or
`.webp` — square, 320×320 or larger — and the photo replaces that person's
initials in the team section. A name with no file here keeps the initials.
