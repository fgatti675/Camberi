import type { PostMeta } from '../journal/post';

/* See the note in `../journal-breaking-changes/meta.ts` — the path and date
   live here so the journal index can list the post without importing a
   component. */
export const rowLevelSecurityMeta: PostMeta = {
  path: '/journal/row-level-security/',
  date: '2026-09-09',
};
